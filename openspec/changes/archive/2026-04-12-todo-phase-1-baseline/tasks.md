# Tasks: Todo Phase 1 - The Baseline

## Implementation Plan

### 1. Initialization
- [x] Create `index.html` file in the root directory.
- [x] Create `style.css` file in the root directory.

### 2. HTML Structure
- [x] Add basic HTML boilerplate with `<link>` to `style.css`.
- [x] Create a `<div>` container with the ID `web-container`.
- [x] Add a `<h1>` title calling it "My Modern Todo".
- [x] Create a `<ul>` list with the ID `todo-list`.
- [x] Add three `<li>` elements: "Buy milk", "Learn SDD", "Practice HTML".

### 3. Styling
- [x] Reset margins and padding for clean layout.
- [x] Center the `#web-container` horizontally.
- [x] Style the list items to have proper spacing and bullet-point removal.

### 4. Verification
- [x] Open `index.html` in the browser.
- [x] Confirm layout is centered and seed tasks are visible.
- [x] Verify typography and spacing looks professional.
